package rover_msgs;

struct RoverStatus {
    string source; //The name of the program sending the message, e.g. "jetson/teleop"
    string message; //The message.

    //Send this in the "/debug" lcm channel for standard messages (status updates, etc...)
    //Send this in the "/warning" lcm channel for low priority errors that will resolve themselves, but the operator should be aware of
    //Send this in the "/error" lcm channel for high priority errors that cannot be resolved automatically. Include recovery instructions for operators.
}